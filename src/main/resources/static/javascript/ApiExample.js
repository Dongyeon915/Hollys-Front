document.addEventListener("DOMContentLoaded", () => {
  // $("h1").addClass('abc'); EL요소에 클래스 추가
  // $("h1").removeClass('abc'); 클래스 제거
  // $("h1").add(`p`).css(`color`,`red`);  .add .css
  // $(`h1`).after(`<p>태그추가after<p>`); 해당 EL에 태그를 추가해주는 .after
  // $(`h1`).hide(1000,'linear'); EL요소를 숨겨주는 .hide 파라미터로 시간과 사라짐 방식을사용 기본은 swing방식
  // $(`h1`).toggle(`slow`) .toggle()은 선택한 요소가 보이면 보이지 않게, 보이지 않으면 보이게 합니다
  // const h1EL = $(`h1`).text(); 해당 EL에 text내용을 가져올수 있는 .text() 태그추가도 가능하다.
  // console.log(h1EL);
 //  var h1EL = $(`h1`).html(); HTML 태그를 포함하여 선택한 요소 안의 내용을 가져옵니다.
 //  console.log(h1EL)
 // $(`h1`).html(`<h1>변경된h1<h1>`);
})
